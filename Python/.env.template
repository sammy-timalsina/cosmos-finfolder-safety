# Cosmos Reason 2 endpoint via SSH tunnel
BREV_ENDPOINT=http://localhost:8000/v1/chat/completions

# Camera index — 0 = first USB camera (Arducam)
CAMERA_INDEX=0

# Audio model paths — update these for your environment
MODEL_PATH=C:\path\to\anomaly_detector.onnx
MIN_VALS_PATH=C:\path\to\min_vals.npy
MAX_VALS_PATH=C:\path\to\max_vals.npy
THRESHOLD=5.155